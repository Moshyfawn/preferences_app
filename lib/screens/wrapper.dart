import 'package:flutter/material.dart';
import 'package:preferences_app/screens/home/home.dart' show Home;

class Wrapper extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // return home / auth
    return Home();
  }
}
